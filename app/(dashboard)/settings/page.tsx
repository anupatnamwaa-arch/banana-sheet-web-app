// Budget & Settings tab — monthly category limits, the api_key for the Shortcut
// (with Regenerate), plan status, CSV import/export, link to Upgrade.
export default function SettingsPage() {
  return (
    <section className="space-y-4">
      <header className="pt-2">
        <h1 className="text-2xl font-semibold tracking-tight">Settings</h1>
      </header>
      <div className="glass p-4 text-sm text-fg-muted">
        API key + Regenerate, Budgets, CSV import/export, plan status — TODO.
      </div>
    </section>
  );
}
