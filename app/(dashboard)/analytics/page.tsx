// Analytics tab (Pro) — Monthly Velocity (glowing gridless line),
// Category Breakdown donut (spend vs budget), transaction CRUD list.
// Charts + CRUD: next task (paused here per plan).
export default function AnalyticsPage() {
  return (
    <section className="space-y-4">
      <header className="pt-2">
        <h1 className="text-2xl font-semibold tracking-tight">Analytics</h1>
      </header>
      <div className="glass p-4 text-sm text-fg-muted">
        Monthly Velocity + Category donut + CRUD list — TODO.
      </div>
    </section>
  );
}
